# Acceptance audit: P0-P3

Snapshot: 2026-09-09. This assesses the [implementation plan](../architecture/windows-client-plan.md), sections 6-9 and phases P0-P3, against current code and completed receipts. It distinguishes implementation, native lifecycle evidence, actual UI observations, and installed-release acceptance. P4 remains optional and outside scope.

Project/interface names in the plan are illustrative boundaries, not a checklist of required assemblies. The present `Api`, `Playback`, and Windows app modules preserve the important separation. A separate `ICredentialStore`, reporter project, DI container, Generic Host, VLC adapter, or extra Toolkit package is not required merely to reproduce an example name. Local verification was explicitly authorized during implementation; the earlier research-stage `test-env` restriction does not invalidate that evidence.

## Phase conclusions

| Phase | Conclusion against its exit condition |
| --- | --- |
| P0 | Engine comparison, protocol slice, native playback, direct/HLS resources, and package construction have evidence. **The full PoC gate has not passed**: final integrated composition/track checks, native external subtitles, accessibility, and installed clean-machine playback remain open. |
| P1 | The requested library and basic player functions are implemented, with earlier real UI observations. **Consistent playback/resume on the final application is not yet accepted**, particularly after the HLS and notification-lifetime changes. |
| P2 | Source/track renegotiation, bounded decoder fallback, episode continuation, scoped SMTC commands, editable transient queue, diagnostics, and a candid capability matrix exist. Queue editing/continuation and diagnostics have actual UI evidence. Wider subtitle/codec and input acceptance remain incomplete. |
| P3 | Bounded caches, protected storage, window/display services, verified bounded manual recovery, CI, notices, SBOM, and unsigned packaging exist. **No reproducible installed release is established**; accessibility/performance, publisher decisions, and clean-machine gates remain. |

## Evidence already appropriate to the requirement

| Plan requirement | Current evidence and its boundary |
| --- | --- |
| P0; section 9 dependency compatibility and engine decision | [Hosted CI run 34301423262](https://github.com/moooyo/emby-client-winui3/actions/runs/34301423262) passed locked restore, Release build, 306 tests, AOT/SBOM publication, unsigned MSIX construction, and artifact upload for `a2e4e43`. [Current status](status.md) records a later 340-test local run; that is a different checkpoint. [VLC receipt](../../tools/EmbyClient.VlcProbe/verification/2026-09-09.json) records its actual XAML failure, supporting the documented [native-engine decision](../architecture/playback-engine-decision.md). |
| Section 6 negotiation, account context, authorization, and conservative capabilities | [API client](../../src/EmbyClient.Api/EmbyApiClient.cs), [request factory](../../src/EmbyClient.Playback/PlaybackRequestFactory.cs), and [profile](../../src/EmbyClient.Playback/ConservativeDeviceProfile.cs) implement source selection, opening/closing requirements, proxy-aware routes, and scoped credentials. [40 official-server checks](../../tools/EmbyClient.ServerValidation/RESULTS.md) exercise Emby 4.9.5.0; [transport tests](../../tests/EmbyClient.MediaTransport.Tests/) cover redirect/range boundaries. This is not a multi-server/proxy matrix. |
| Section 6 reporting, timeline, fallback, cancellation, and cleanup | [Coordinator](../../src/EmbyClient.Playback/PlaybackCoordinator.cs) and its [132-test suite](../../tests/EmbyClient.Playback.Tests/) cover actual-start reporting, periodic/state reports, absolute ticks, paused restarts, one justified decoder fallback, independent cleanup, and explicit recovery ownership. [Notification-owner tests](../../tests/EmbyClient.Platform.Tests/PlaybackNotificationOwnerTests.cs) cover stale UI notifications and automatic-advance ownership. Unit tests establish these control contracts, not native rendering. |
| Section 9 direct playback and lifetime baseline | [Default-owner Direct/SMTC receipt](../../tools/EmbyClient.NativeProbe/verification/default-owner-direct-nativeaot.json): 20 product cycles, native clock/dimensions, pause/seek/resume, ordered reports, media-request retirement, and SMTC cleanup; +17 handles and +2,166,784 private bytes pass the original limits. It uses muted synthetic H.264/AAC MP4 and does not establish audible output or displayed pixels. |
| Section 9 forced transcode and session correctness | [Default-owner real HLS receipt](../../tools/EmbyClient.NativeProbe/verification/default-owner-real-hls-nativeaot.json): 20 functional cycles, 0/17-second starts, actual replacement positions at 45 seconds, pause restoration, and 40 encoding cleanups. **The unchanged resource gate passes at -6 handles and +720,896 private bytes.** Historical failures and isolated controls remain retained separately. |
| P1 library and basic playback UI | [Library view model](../../src/EmbyClient.App/ViewModels/LibraryViewModel.cs), [library view](../../src/EmbyClient.App/Views/LibraryView.xaml), and [player](../../src/EmbyClient.App/Views/PlayerView.xaml.cs) implement the listed P1 flows. [Earlier UI observations](ui-validation.md) include sign-in/restore, search, favorites/watched state, series navigation, video, and automatic next episode. Their executable predates later playback changes. |
| Section 7 credentials, device identity, and scoped caches | [Account store](../../src/EmbyClient.App/Services/AccountStore.cs) and [connection service](../../src/EmbyClient.App/Services/ConnectionService.cs) have actual Windows protection/persistence tests. [Image cache](../../src/EmbyClient.App/Services/ImageCache.cs) bounds memory to 32 MiB/128 entries with four requests and server/root/user/image keys; [cache tests](../../tests/EmbyClient.Platform.Tests/ImageCacheTests.cs) cover isolation and eviction. No disk image cache is implemented, so there is no unbounded disk cache to fix. |
| Section 7 Windows integration | [SMTC bridge](../../src/EmbyClient.App/Playback/NativePlaybackEngine.MediaControls.cs), [display-request ownership](../../src/EmbyClient.App/Services/PlaybackDisplayRequest.cs), and [window-placement store](../../src/EmbyClient.App/Services/WindowPlacementStore.cs) are integrated. Policy/storage tests establish ownership; the 0251D189 UI checkpoint also verifies close/relaunch position restoration. Physical keys, actual display sleep, and multi-monitor/DPI behavior remain separate. |
| Section 8 dependency accounting and package construction | [Inventory](dependency-inventory.md), [retained notices](../../licenses/third-party/SOURCES.md), [SBOM generator](../../scripts/Generate-Sbom.ps1), and [packager](../../scripts/Package.ps1) exist. The recorded unsigned MSIX preserves 287 resource keys and 332 payload files; its review explicitly leaves signing, installation, and packaged runtime false. [Packaging scope](packaging.md) keeps this distinction explicit. |

## Remaining work within implementation/verification scope

These are not all missing code. A failed or absent acceptance receipt must first be resolved with the relevant implementation or observation.

| Requirement | Concrete remaining work |
| --- | --- |
| P0/P1; section 9 lifetime | Product direct/HLS resource repair and [cancellation/failure/stale-callback isolation](../../tools/EmbyClient.NativeProbe/verification/default-owner-isolation.json) have passed. Complete app closure while opening and playing on the final integrated path. Later transport changes require relevant focused verification without rewriting the historical receipts. |
| P1; section 9 composition, server delivery, and tracks | Recheck the final published app: controls/menus/dialogs/captions, two audio tracks, versions, paused changes, and source-frame agreement after resumed HLS. Current receipts prove original DirectStream and forced conversion; no separately identified remux-rendering pass should be inferred. The explicit external SRT/WebVTT gate is not satisfied by an API download or server-burned subtitle pixels: verify the native external path or record an explicit scope change. |
| P2; section 9 complex subtitles/color/audio | Add separate fixture results for ASS/fonts and PGS, verifying the server fallback or declaring the case unsupported. Preserve the narrow measured SDR baseline. Broader codecs and output modes need individual evidence, not a larger advertised profile. [Capability limits](capabilities.md) already avoid universal support claims. |
| P2 queue and diagnostics | A 100-item transient queue has native inspection/removal/reordering UI and consumption after successful detail preparation. Fourteen queue tests cover failed/cancelled preparation and edits during loading. The bounded diagnostic service has six tests. C6E8E17B actual UI verifies queue editing/automatic consumption and diagnostics copy/save/refresh. No P4 persistent playlist or automatic upload is added. |
| P3 recovery | The [actual cold-range probe](../../tools/EmbyClient.NativeProbe/verification/network-retry-nativeaot.json) now passes classification, new-session recovery at 90.01 seconds with pause preserved, subsequent resume, and cleanup. It exposed and fixed relay classification and default-track intent defects; both earlier failures remain retained. 313A94C3 also passes actual manual Retry after a negotiation 503 from a three-second resume target. A failed seek's native cursor is not evidence of a decoded frame. Broader outage scenarios remain separate. |
| P3 large libraries and accessibility | Virtualized `GridView`/`ItemsWrapGrid`, pagination, image sizing, and recycled-cell cancellation are present. The actual 5,000-item fixture trial loaded bounded pages and revisited content, but private memory continued rising through the short run; investigate before closing performance acceptance. Complete keyboard/Narrator/focus/contrast checks. Cache tests and UI Automation names cannot close these gates. |
| Section 8 CI/release automation | The [workflow](../../.github/workflows/ci.yml) builds/tests/publishes an unsigned AOT folder and constructs/uploads an unsigned MSIX. Hosted run 34301423262 succeeded for a2e4e43; it predates the new UI and network fix. Disposable-server, interactive, install/upgrade/uninstall, and signing checks retain distinct receipts. Later revisions need their own CI result. |
| P3 updater strategy | [The initial update policy](update-policy.md) covers manual development-folder replacement, release-channel choices, bundled-runtime servicing, and compatible rollback. An automatic updater implementation is explicitly deferred by the plan. |

## Environment and publisher gates

- **Interactive facilities:** physical media keys are unsupported by the current input tool. Minimize/restore, suspend/resume, 100/150/200% DPI moves, focus restoration, actual display sleep, and audio-device transitions still need suitable Windows sessions/devices. HDR/color and audible/multichannel/passthrough claims need appropriate output hardware and observations. Some checks may be completed on the available machine; absence of evidence alone is not proof that the environment is blocked.
- **Installation facilities and identity:** choose a publisher identity/certificate and an authorized clean Windows installation environment before signing, changing trust, installing, upgrading, or uninstalling MSIX. Neither `makeappx` success nor the self-contained developer folder proves package activation or native dependency discovery after installation.
- **Owner decisions:** select the repository license, complete concrete redistribution review, choose release channels and a tested Windows/server support matrix. The development publisher placeholder and collected third-party notices do not make these decisions. Store publication and protected signing credentials remain release-owner actions.

The 0251D189 UI checkpoint successfully verified paused 17-second HLS frames, English/French audio changes, source-version changes, visible server-burned SRT at 41 seconds, and window-position restoration. It predates the final shared owner and retry/queue/diagnostic UI. Existing successful receipts remain scoped to their exact artifacts. P0-P3 should not be marked complete by adding together unrelated unit tests, isolated controls, and unsigned package structure checks.
